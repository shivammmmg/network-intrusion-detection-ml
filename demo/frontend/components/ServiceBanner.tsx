export function ServiceBanner() {
  return (
    <section className="service-banner" role="status">
      <strong>Live inference unavailable</strong>
      <p>The evaluation results and methodology below remain available, but live model predictions are temporarily offline.</p>
    </section>
  );
}
