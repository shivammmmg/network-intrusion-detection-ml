/**
 * Display order for the Live Detection model selector and agreement preview
 * only. This is presentation-only — it does not affect the backend model
 * registry order, API responses, or the Compare Models page.
 */
export const LIVE_MODEL_ORDER = ["random_forest", "xgboost", "neural_network", "logistic_regression"];
