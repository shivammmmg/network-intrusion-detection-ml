# Golden fixtures

`golden_examples.json` stores predictor-only records derived from frozen held-out
test rows at source SHA `4b8165aaaaaf00d4ed9a551e5f07ea38c7cb0072`.

Known labels, committed probabilities, and locked-threshold outcomes are display
and test metadata only. Inference receives only each example's `record`.
